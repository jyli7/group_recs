%h1 Your Friends
=form_for :group_recs, :url => user_group_recs_path(@user) do |f|
	#friends_container
		-@friends.each do |friend|
			.friend_block
				.check_box= f.check_box friend.user_id
				.friend_img= image_tag(friend.image_url)				
				.name= friend.name
	=f.submit
	